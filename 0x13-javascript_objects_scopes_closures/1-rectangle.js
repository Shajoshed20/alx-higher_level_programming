#!/usr/bin/node
// A Rectangle class that defines a rectangle

module.exports = class Rectangle {
  width;
  height;

  constructor (width, height) {
    this.width = width;
    this.height = height;
  }
};
